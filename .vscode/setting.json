{
      "path-autocomplete.pathMappings": {
            "@": "${folder}/src",
            "R": "${folder}"
      },
      "path-intellisense.mappings": {
            "@": "${workspaceRoot}/src",
            "R": "${workspaceRoot}"
      }
}